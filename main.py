from app.count_accessible_cells_for_ant import count_accessible_cells_for_ant
from app.count_accessible_cells_for_ant import Point

if __name__ == "__main__":
    print(count_accessible_cells_for_ant(Point(1000, 1000)))
